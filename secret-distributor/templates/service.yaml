apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-distributor.fullname" . }}
  labels:
    {{- include "secret-distributor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-distributor.selectorLabels" . | nindent 4 }}
